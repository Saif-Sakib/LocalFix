import React from 'react';

const Footer = () => (
    <footer className="footer">
        <p>&copy; {new Date().getFullYear()} LocalFix. Empowering communities, one fix at a time.</p>
    </footer>
);

export default Footer;
